= form_with(model: checklist, local: true) do |form|

  - if checklist.errors.any?
    #error_explanation
      %h2
        = pluralize(checklist.errors.count, "error")
        prohibited this checklist from being saved:
      %ul
        - checklist.errors.full_messages.each do |message|
          %li= message
          
  .field
    = form.label :title
    = form.text_field :title, required: 'required'
    
  .field
    = form.label :description
    = form.text_area :description, required: 'required'
  .actions
    = form.submit
